# คำอธิบายปุ่ม Dark Mode / Light Mode

## โครงสร้างและการทำงานของปุ่ม Dark Mode / Light Mode

ปุ่ม Dark Mode / Light Mode เป็นองค์ประกอบที่สำคัญในเว็บไซต์ E-learning ซึ่งช่วยให้ผู้ใช้งานสามารถเปลี่ยนโหมดการแสดงผลของเว็บไซต์ได้ตามต้องการ โดยมีการออกแบบให้ใช้งานง่ายและมีการแสดงผลที่ชัดเจน

### โครงสร้างหลักของปุ่ม Dark Mode / Light Mode

1. **ส่วนแสดงผล (Display Section)**
   - แสดงปุ่มสลับโหมดในรูปแบบ Toggle Switch
   - มีการแสดงไอคอน ☀️ สำหรับ Light Mode และ 🌙 สำหรับ Dark Mode
   - มีการออกแบบให้เข้ากับธีมของเว็บไซต์

2. **ส่วนควบคุม (Control Section)**
   - มีการควบคุมการเปลี่ยนโหมดผ่านการคลิกที่ปุ่ม
   - รองรับทั้งโหมด Light และ Dark

3. **ส่วน Effects**
   - มี Animation ในการเปลี่ยนโหมด
   - มี Glowing Effect และ Snow Effect สำหรับปุ่ม

### การทำงานของปุ่ม Dark Mode / Light Mode

1. **การเริ่มต้นระบบ**
   - เมื่อโหลดหน้าเว็บ ระบบจะตรวจสอบการตั้งค่าของผู้ใช้
   - หากไม่มีการตั้งค่า ระบบจะใช้โหมด Light เป็นค่าเริ่มต้น
   - ปุ่มจะแสดงสถานะโหมดปัจจุบันที่ใช้งานอยู่

2. **การเปลี่ยนโหมด**
   - เมื่อผู้ใช้คลิกที่ปุ่ม Dark Mode ระบบจะเปลี่ยนโหมดทันที
   - ทุกส่วนของเว็บไซต์จะเปลี่ยนตามโหมดที่เลือก
   - สถานะของปุ่มจะเปลี่ยนไปแสดงโหมดที่เลือกใหม่

3. **การจัดการ Theme**
   - ใช้ CSS Variables ในการจัดการสีของธีม
   - ระบบจะบันทึกการตั้งค่าโหมดไว้ใน localStorage เพื่อใช้ในครั้งถัดไป

### การตกแต่งด้วย CSS

1. **การออกแบบปุ่ม**
   - ใช้ CSS ในการออกแบบปุ่มในรูปแบบ Toggle Switch
   - มีการออกแบบให้เข้ากับธีมของเว็บไซต์ทั้ง Light และ Dark

2. **CSS Variables**
   - ใช้ CSS Variables ในการจัดการสีของธีม เช่น `--background`, `--foreground`, `--primary`
   - มีการกำหนดค่าต่างๆ สำหรับทั้งโหมด Light และ Dark

3. **Animation และ Effects**
   - มี Animation ในการเลื่อนปุ่มเมื่อเปลี่ยนโหมด
   - มี Glowing Effect รอบปุ่มเพื่อให้เด่นชัด
   - มี Snow Effect สำหรับโหมด Dark
   - มี Transition ในการเปลี่ยนสีของปุ่ม

4. **Responsive Design**
   - ปุ่มจะอยู่ในตำแหน่งมุมล่างขวาของหน้าจอ
   - รองรับการแสดงผลบนทุกขนาดหน้าจอ

### เทคโนโลยีที่ใช้

1. **React Hooks**
   - ใช้ useState ในการจัดการสถานะโหมด
   - ใช้ useEffect ในการตรวจสอบการตั้งค่าเริ่มต้น

2. **CSS3**
   - ใช้ CSS Variables สำหรับ Theme
   - ใช้ Flexbox ในการจัดวางองค์ประกอบ
   - ใช้ Animation และ Transition สำหรับ Effects
   - ใช้ Keyframes สำหรับ Animation แบบกำหนดเอง

3. **LocalStorage**
   - ใช้ localStorage ในการบันทึกการตั้งค่าโหมดของผู้ใช้
   - ทำให้ผู้ใช้ไม่ต้องตั้งค่าใหม่ในแต่ละครั้งที่เข้าเว็บ

### ประโยชน์ของการใช้ TypeScript

1. **Type Safety**
   - ช่วยป้องกันข้อผิดพลาดในการจัดการสถานะโหมด
   - ทำให้โค้ดอ่านเข้าใจง่ายขึ้น

2. **IntelliSense**
   - ช่วยในการพัฒนาด้วยการแนะนำโค้ดอัตโนมัติ
   - ลดเวลาในการพัฒนาและลดข้อผิดพลาด

3. **การบำรุงรักษา**
   - ทำให้โค้ดมีโครงสร้างที่ชัดเจน
   - ง่ายต่อการปรับปรุงและแก้ไขในอนาคต โดยเฉพาะเมื่อมีการเพิ่มโหมดใหม่